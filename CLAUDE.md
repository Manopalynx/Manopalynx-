<!--
Pointer file only. It exists so the two files below load automatically at the start
of a session rather than having to be asked for. Keep content in those files, not here.

  workingwithsam.md  the running handover between sessions, and the map of which
                     project is which. Read it first.
  README.md          the ledger's documentation.

The other two projects carry their own documents -- docs/README.md for Grandiose,
MATCHBOX.md for matchbox. Those are deliberately not imported: loading all three
every session would cost far more context than it saves. Read the one you need.
-->

@workingwithsam.md

@README.md
